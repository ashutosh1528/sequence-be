import { InMemoryDataStore } from "../datastore/InMemoryDataStore";

export class GameRoom {
  constructor() {}

  createGame() {
    // const dataStoreInstance = InMemoryDataStore.getInstance();
    // const game = dataStoreInstance.createGame();
    // return game;
  }

  addPlayers(gameId: string) {}
}
